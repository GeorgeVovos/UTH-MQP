#include <stdio.h>
int main() {
     printf("In PThreads we don't have a built-in concept of shared/private variables. You can use global variables as shared data and parameters as private ones\n");
    return 0;
}
